# API URL (for production deployment)
VITE_API_URL=https://your-backend-url.onrender.com/api

# Note: During development with Vite proxy, you don't need to set this.
# The proxy is configured in vite.config.js to forward /api requests to localhost:5000
